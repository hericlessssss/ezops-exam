# SECURITY NOTICE: Do not commit actual secrets to git!
# Create secrets manually using kubectl before deploying:
#
# kubectl create secret generic backend-secrets \
#   --namespace test-chico \
#   --from-literal=DB_USER='<YOUR_DB_USER>' \
#   --from-literal=DB_PASSWORD='<YOUR_DB_PASSWORD>'

apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: test-chico
type: Opaque
data:
  # Base64 encoded values would go here if not creating efficiently via CLI
  # DB_USER: ...
  # DB_PASSWORD: ...
